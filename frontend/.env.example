# API base URL — leave empty to use relative /api (same domain via reverse proxy)
# Set this if frontend and backend are on different domains
VITE_API_URL=

# Backend target for Vite dev server proxy
VITE_API_TARGET=http://localhost:3001
